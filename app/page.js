
'use client';
import {useEffect,useState} from 'react';import {seedMatches,getFlag,getKu,closed} from '../lib/data';
function load(k,d){if(typeof window==='undefined')return d;return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}function save(k,v){localStorage.setItem(k,JSON.stringify(v))}
export default function Home(){const [matches,setMatches]=useState([]),[preds,setPreds]=useState([]),[person,setPerson]=useState({name:'',phone:'',apt:''}),[scores,setScores]=useState({});useEffect(()=>{setMatches(load('matches',seedMatches));setPreds(load('predictions',[]));},[]);function submit(m){if(!person.name||!person.phone||!person.apt)return alert('تکایە ناو، ژمارەی مۆبایل و ژمارەی شوقە بنووسە'); if(closed(m.kickoff))return alert('پێشبینی بۆ ئەم یارییە داخراوە'); const key=person.phone+'-'+person.apt+'-'+m.id;if(preds.some(p=>p.key===key))return alert('تۆ بۆ ئەم یارییە پێشتر پێشبینیت کردووە'); let s=scores[m.id]||{}; if(s.a===''||s.b===''||s.a==null||s.b==null)return alert('تکایە ئەنجامی پێشبینی بنووسە'); const n=[...preds,{key,match:m.id,name:person.name,phone:person.phone,apt:person.apt,a:+s.a,b:+s.b,created:new Date().toISOString()}];setPreds(n);save('predictions',n);alert('پێشبینییەکەت تۆمار کرا ✅')}
return <main className="kurdish"><section className="hero container"><div className="nav"><div className="brand"><div className="logo">MC</div><div><h3>Marina City</h3><div className="small">World Cup 2026 Predictor</div></div></div><a className="pill" href="/admin">Admin</a></div><div className="heroCard"><span className="pill">Marina City × World Cup 2026</span><h1>پێشبینی بکە، ناوت بچێتە ڕولێت</h1><p className="sub">بۆ هەر یارییەک پێشبینی ئەنجام بکە. پێشبینی یەک کاتژمێر پێش دەستپێکی یاری دادەخرێت. دوای ئەنجامی کۆتایی، ئەوانەی ڕاستیان کردووە دەچنە ڕولێت.</p></div></section><section className="container section grid"><div className="card"><h2>تۆمارکردن</h2><div className="row3"><input placeholder="ناوی تەواو" onChange={e=>setPerson({...person,name:e.target.value})}/><input placeholder="ژمارەی مۆبایل" onChange={e=>setPerson({...person,phone:e.target.value})}/><input placeholder="ژمارەی شوقە" onChange={e=>setPerson({...person,apt:e.target.value})}/></div><p className="small">بۆ ڕێگری لە دووبارە پێشبینی، ژمارەی مۆبایل و شوقە بەکاردێت.</p></div>{matches.map(m=><div className="card half" key={m.id}><div className="match"><div className="team"><span className="flag">{getFlag(m.a)}</span><b>{getKu(m.a)}</b></div><div className="vs">VS</div><div className="team"><span className="flag">{getFlag(m.b)}</span><b>{getKu(m.b)}</b></div></div><p className="status">{m.round} · {new Date(m.kickoff).toLocaleString('ar-IQ')} · {closed(m.kickoff)?'پێشبینی داخراوە':'پێشبینی کراوەیە'}</p><div className="row"><input type="number" min="0" placeholder={`گۆڵی ${getKu(m.a)}`} onChange={e=>setScores({...scores,[m.id]:{...(scores[m.id]||{}),a:e.target.value}})}/><input type="number" min="0" placeholder={`گۆڵی ${getKu(m.b)}`} onChange={e=>setScores({...scores,[m.id]:{...(scores[m.id]||{}),b:e.target.value}})}/></div><br/><button onClick={()=>submit(m)} disabled={closed(m.kickoff)}>پێشبینی بنێرە</button>{m.finalA!=null&&<p className="winner">ئەنجام: {m.finalA} - {m.finalB}</p>}</div>)}</section><footer className="footer">Marina City World Cup 2026</footer></main>}
